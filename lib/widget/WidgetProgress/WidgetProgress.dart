import 'package:flutter/material.dart';

class WidgetProgress extends StatelessWidget {
  WidgetProgress({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('Laporan Progress'),
      ),
    );
  }
}
