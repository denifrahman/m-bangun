import 'package:flutter/material.dart';

class PreviewAndSaveImage extends StatelessWidget {
  PreviewAndSaveImage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('Preview and Save'),
      ),
    );
  }
}
